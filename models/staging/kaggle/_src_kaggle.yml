version: 2

sources:
  - name: kaggle
    description: "These datasets come from Kaggle"
    database: "{{ env_var('DBT_ENVIRONMENTS') }}_BRONZE"
    tables:
      - name: MENTAL_HEATLH_AND_SOCIAL_MEDIA_BALANCE
        description: "This table contains information about wellbeing and social media behaviour of a given user"
        columns:
          - name: user_id
            data_type: varchar
            description: "User id"

          - name: age
            data_type: number
            description: "User age"

          - name: gender
            data_type: varchar
            description: "User gender"

          - name: daily_screen_time_hrs
            data_type: varchar
            description: "Daily screen time hours of user"

          - name: "Sleep_Quality(1-10)"
            data_type: number
            description: "Sleep quality (1–10)"
            
          - name: "Stress_Level(1-10)"
            data_type: number
            description: "Stress level (1–10)"
            
          - name: days_without_social_media
            data_type: number
            description: "Days in a row user has not been in any social media platform."

          - name: "exercise_frequency(week)"
            data_type: number
            description: "How many times a week a user does exercise"

          - name: social_media_platform
            data_type: varchar
            description: "Social media platform preference by user."

          - name: "Happiness_Index(10)"
            data_type: number
            description: "Happiness index (1–10)"
            
      - name: student_performance
        description: "Student performance in school"
        columns:
          - name: student_id
            data_type: varchar
          - name: hours_studied
            data_type: number
          - name: sleep_hours
            data_type: number
          - name: attendance_percent
            data_type: number
          - name: previous_scores
            data_type: number
          - name: exam_score
            data_type: number

      - name: marketing_campaigns
        description: "Marketing campaigns"
        columns:
          - name: campaign_id
            data_type: varchar

          - name: campaign_type
            data_type: varchar
            tests:
              - accepted_values:
                  arguments:
                    values:
                      - "Email"
                      - "Social Media"
                      - "Push Notification"
                      - "Search Ads"
                      - "Display Ads"

          - name: topic
            data_type: varchar
          - name: target
            data_type: varchar
          - name: sent_at
            data_type: timestamp_tz